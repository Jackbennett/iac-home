apiVersion: apps/v1
kind: Deployment
metadata:
  name: directus-deployment
  labels:
    app: directus
spec:
  selector:
    matchLabels:
      app: directus
  template:
    metadata:
      labels:
        app: directus
    spec:
      containers:
      - name: directus
        image: directus/directus:11.13
        ports:
          - containerPort: 80
        env:
          - name: PUBLIC_URL
            value: https://cms.jackben.net
          - name: TELEMETRY
            value: false
          - name: ACCEPT_TERMS
            value: false
          - name: CONFIG_PATH
            value: /run/directus/config.yaml
        volumeMounts:
          - name: tmp
            mountPath: /tmp
          - name: config
            mountPath: /run/directus
      volumes:
        - name: tmp
          emptyDir: {}
        - name: config
          secret:
            secretName: cms-directus-config
            items:
              - key: config.yaml
                path: config.yaml
